export class QueryInput {
  sequence: string;

  database: string;
 constructor(sequence: string, database: string) {
 this.database = database;
 // nope
 // nope
 this.sequence = sequence;
 // this.species = species;
 }

}
